#include<stdio.h> 

int main()
{
    int a,b,i,n
    printf("Enter the total numbers you want :");
    scanf("%d",&n);

    for(i=1;i<=n;i++)
    {
        scanf("%d",num[i]);
    }

    min=num[0];
    for(i=1;i<=n;i++)
    {
        if(num[i]<max)
            min=num[i];
    }

    while(min>1)
    {
        if(num[i]%min==0)
            
    }
    scanf("%")
    return 0; 
}